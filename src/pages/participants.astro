---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import logoDefault from "../assets/logoDefault.png";
import {
  borderGradientButtonClass,
  gradientButtonClass,
} from "../helpers/constants";
import BackButton from "../components/BackButton.astro";
import ResumenParticipants from "../components/ResumenParticipants";

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<!-- class="h-full w-full bg-linear-to-b from-[#1B262E] to-[#577A94] flex flex-col items-center justify-between p-10" -->
<Layout>
  <main
    class="h-full w-full bg-linear-to-b from-primary to-black grid grid-cols-1 gap-4 px-6 py-16"
  >
    <BackButton />
    <section class="flex flex-col items-center justify-center">
      <ResumenParticipants client:load />
    </section>
  </main>
</Layout>
